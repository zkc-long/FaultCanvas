# FaultCanvas

Programmable HTTP fault proxy and stateful service-virtualization gateway for
MoonBit. The public library provides deterministic request matching, scenario
state, fault planning, configuration decoding and evidence reports; the native
adapter executes decisions on a real HTTP/1.1 boundary.

See the repository `README.md` for installation and examples.
